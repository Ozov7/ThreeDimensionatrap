# Drosera Security Traps Configuration
# Register ONLY the orchestrator, not individual traps

[trap.security_orchestrator]
address = "0x..."  # Replace with orchestrator address after deployment
response_contract = "0x..."  # Replace with responder address after deployment
response_function = "handleCrossVectorAlert"
cooldown_period_blocks = 10
min_number_of_operators = 1
max_number_of_operators = 3
block_sample_size = 5
private_trap = false
whitelist = []
